{
  "name": "ecoalert-scripts",
  "version": "1.0.0",
  "private": true,
  "description": "Standalone one-off Supabase admin scripts (migrations, first-admin seeding) — not part of the deployed Next.js app.",
  "scripts": {
    "db:migrate": "ts-node migrate.ts",
    "seed:admin": "ts-node seedAdmin.ts"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.112.2",
    "dotenv": "16.4.5",
    "postgres": "^3.4.9"
  },
  "devDependencies": {
    "@types/node": "20.14.15",
    "ts-node": "10.9.2",
    "typescript": "5.5.4"
  }
}
